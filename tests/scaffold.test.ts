/**
 * Copyright (c) 2026 Sajid Ahmed
 */
import { describe, expect, it } from 'vitest';

// Trivial sanity test proving the new `tests/**/*.test.ts` glob resolves.
describe('scaffold', () => {
    it('resolves the tests/ glob', () => {
        expect(true).toBe(true);
    });
});
